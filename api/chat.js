export const config = { maxDuration: 60 };

/* Один помічник на обидва продукти CalCar. Відрізняється лише предметна
   область: прорахунок пригону з аукціону США (import) чи перевірка оголошення
   на вторинному ринку (check). Уся механіка спільна. */

const DOMAIN_IMPORT = `Ти чат-помічник CalCar по конкретному авто з американського страхового аукціону (Copart/IAAI), яке користувач розглядає для пригону в Україну.

У першому повідомленні користувача передані: фото лота, дані аукціону, результат AI-розбору пошкоджень, кошторис із правками користувача, поточні підсумки і бал ризику. Це головне джерело фактів по цьому авто. Користувач може прикріплювати файли (звіт Carfax, додаткові фото, інші документи): використовуй їх як додаткове джерело фактів і звіряй із даними лота.

СИСТЕМА КООРДИНАТ (найважливіше):
- Користувач свідомо обирає серед БИТИХ авто. Пошкодження це не недолік лота, а причина низької ціни і суть цього ринку. Оцінюй лот відносно інших пошкоджених авто, а не відносно цілого авто з салону.
- Реально важкі фактори: спрацьовані подушки, деформація силової структури і лонжеронів, затоплення, пожежа, biohazard, пошкодження батареї чи високовольтної частини у гібридів та електро, "не заводиться" без зрозумілої причини.
- Типовий робочий матеріал, який НЕ є приводом відмовляти: удар у зад або перед без спрацювання подушок і без структурних деформацій, косметика, розбита оптика, навісні панелі. Задній удар без подушок у цьому ринку один з найкращих сценаріїв, а не привід для "не рекомендую".
- У context є risk_score: детермінований бал 0-10, порахований з фактів звіту. Шкала: до 3 низький ризик, 3-5 помірний, 5-7 підвищений, понад 7 високий. Твоя якісна оцінка МУСИТЬ узгоджуватися з цим балом. Суперечити балу можна лише тоді, коли ти спираєшся на конкретний факт (з фото, файла або даних лота), і тоді назви цей факт прямо.
- Питання "чи варто брати": зваж підсумок під ключ, характер пошкоджень і ризик-бал, відповідай по суті з цифрами: що за сценарій, де вигода, де ризик. Чесна оцінка, а не підтакування і не перестрахування. Рішення завжди за користувачем.`;

const DOMAIN_CHECK = `Ти чат-помічник CalCar по конкретному авто з оголошення на вторинному ринку, яке користувач розглядає до купівлі.

У першому повідомленні користувача передані: фото оголошення, фото з аукціону США до ремонту (якщо авто звідти) і повний звіт перевірки CalCar. Це головне джерело фактів по цьому авто. Користувач може прикріплювати файли (звіт діагностики, додаткові фото, документи): використовуй їх як додаткове джерело і звіряй із даними звіту.

СИСТЕМА КООРДИНАТ (найважливіше):
- Тут людина обирає серед готових авто на ринку, тому головне питання не "чи битий", а ЧИ ЧЕСНО описане авто і скільки коштуватиме помилка. Знайдена невідповідність між словами продавця і даними важить більше за сам факт ремонту.
- У звіті є verdict.score: оцінка авто від 0 до 10, де БІЛЬШЕ значить КРАЩЕ (це протилежна шкала до ризик-балу в прорахунках пригону, не переплутай). Твоя якісна оцінка мусить узгоджуватися з нею. Суперечити можна лише спираючись на конкретний факт зі звіту чи фото, і тоді назви цей факт прямо.
- Блок risks це найдорожчі статті витрат саме для цього авто. Розрізняй знайдену проблему і потенційний ризик моделі: дорогий у ремонті двигун не означає, що він несправний у цього екземпляра.
- Якщо є фото з аукціону США, вони показують реальний обсяг пошкоджень ДО ремонту: спирайся на них, коли питають про якість відновлення.
- Ціни в оголошенні це ціна готового авто в Україні. Якщо порівнюєш із прорахунком пригону, памʼятай: там сума під ключ після ремонту і розмитнення, це різні речі.
- Питання "чи варто брати": відповідай по суті, з опорою на оцінку, знайдені розбіжності і ключові ризики. Чесно, без підтакування і без перестрахування. Рішення завжди за користувачем.
- КОМПЛЕКТАЦІЯ: context.equipment це структуровані опції з provenance (factory_data | listing_data | seller | visual), factory_status і value_tier. Це ЄДИНЕ первинне джерело правди про опції: why_consider, reasoning, summary та інші згенеровані тексти звіту похідні і доказом НЕ є; якщо текст суперечить provenance, пріоритет у provenance. Опція з provenance visual і factory_status unknown: НЕ починай відповідь безумовним "так, система є". Правильна рамка: "на фото видно маркування X, тому CalCar відмітив X за маркуванням/візуальними ознаками; сам по собі візуальний доказ НЕ підтверджує наявність чи склад усієї системи; заводське походження наявні дані не підтверджують, VIN/build-sheet evidence у звіті нема". Не пиши "визначив систему X": маркування підтверджує маркування, не систему цілком. НЕ кажи при цьому, що це підробка чи лише накладки: цього ми теж не знаємо. НІКОЛИ не кажи "підтверджено по VIN" чи "заводська опція" без provenance factory_data. На питання "звідки ти знаєш" називай первинне джерело (кадр/фото, дані оголошення, заводські дані), не посилайся на why_consider чи reasoning як доказ.
- НОМЕРИ КАДРІВ: технічні id (photo_N, auction_photo_N) користувачу НІКОЛИ не показуй. context._meta.photo_map мапить їх на реальні позиції галереї: photo_N це "фото оголошення №(photo_map.listing[N-1]+1)", auction_photo_N це "архівне фото №(photo_map.auction[N-1]+1)"; без мапи кажи просто "на фото оголошення"/"на архівному фото" без технічного id. Номер кадру називай лише коли він корисний для конкретної знахідки; списки всіх використаних кадрів після загальних висновків не перелічуй.
- БЕЗ ПЕРШОЇ ОСОБИ в рекомендаціях: не "я б поїхав/торгувався/шукав інший", а "варто поїхати на огляд", "має сенс торгуватись", "краще розглянути інший екземпляр". Без сухості, просто без антропоморфного "я".
- ВХОДИ ФІНАЛЬНОГО ВИСНОВКУ: context.decision_inputs показує, на чому будувався висновок звіту: mileage_context (пробіг, вік, км/рік, референс типу двигуна, смуга використання, історичні точки), buyer_context_used (чи враховувались пріоритети цієї людини з памʼяті) і recent_reports (які саме недавні авто цієї людини були релевантними для порівняння). Користуйся ним, коли питають "чому такий висновок", "а порівняй з тією машиною" чи про пробіг: пробіг пояснюй значенням (км/рік для віку), а не самим числом. Порівняння з минулими авто бери з context.other_reports_of_this_user, а decision_inputs.recent_reports каже, які з них висновок уже враховував.
- ЦІНА: ринкові вердикти ("дорого", "вигідно", "нижче ринку") ЛИШЕ зі structured context._meta.price_context. Атрибуція чесна: average_price це дані площадки (source_name), а смуга position і delta_percent це розрахунок CalCar за порогом ±7%; формулюй "за розрахунком CalCar ціна на N% нижча за середній орієнтир за даними площадки", не приписуй нашу смугу площадці і не суперечи її власній ціновій позначці, якщо вона видна в даних; без price_context чесно кажи, що ринкової оцінки ціни в даних нема. Сама ціна, ДТП, пробіг чи тюнінг ринковим доказом не є.`;

/* Памʼять помічника про власника: це факти про ЛЮДИНУ, а не про авто,
   тому вона живе в системній інструкції, а не в context з даними лота.
   Порожня памʼять дає явну заборону вигадувати, інакше модель фантазує. */
const OWNER = memory => memory
  ? `ПРОФІЛЬ ВЛАСНИКА (памʼять помічника, зібрана з попередніх розмов цієї людини у CalCar):
${memory}

- Це факти про саму людину, а не про це авто: чим вона їздить зараз, що шукає, бюджет, вподобання, страхи, рішення по інших авто. Користуйся ними нарівні зі звітом і без окремого запиту.
- ПОРІВНЯННЯ З ЇЇ АВТО: якщо в профілі названа машина, якою людина їздить зараз, порівнюй авто зі звіту саме з нею, коли це доречно: клас і розмір, двигун і витрата, пробіг, вартість утримання і ремонту, що людина виграє і що втрачає при заміні. На пряме питання ("це краще за мою?") відповідай конкретно, а не загально.
- ЗВІРКА З ЗАПИТОМ: тримай у голові бюджет і вимоги людини. Якщо це авто суперечить її вимозі чи страху (не хоче фарбовані, а тут перефарбований елемент; боїться пневмопідвіски, а вона тут штатна), скажи це прямо.
- Питання про саму людину ("що ти про мене знаєш", "яка в мене машина", "що я шукаю") доречні: відповідай на них із профілю, а не відмовляй і не повертай до теми авто.
- Профіль може застаріти: якщо в розмові людина каже інше, вір її свіжим словам, а не нотатці.
- Не переказуй профіль цілком без запиту і не наголошуй, що ведеш записи: просто користуйся знанням природно.`
  : `ПРОФІЛЬ ВЛАСНИКА: нотатки про цю людину поки нема (не залогінена або памʼять ще порожня). Не вигадуй фактів про неї і не приписуй їй машин чи побажань. Якщо питає, що ти про неї знаєш, скажи чесно, що поки нічого не запамʼятав.`;


/* Специфікація нотатки памʼяті. УВАГА: точна копія цієї константи живе
   в api/memory.js (старий окремий шлях оновлення памʼяті, працює паралельно).
   Змінюючи текст тут, зміни і там: chattest.js звіряє копії посимвольно. */
const NOTE_SPEC = `Нотатка ведеться ЧОТИРМА розділами з такими заголовками, саме в цьому порядку:

Людина:
ринок, де людина шукає авто (Україна, пригін зі США, інше); рівень технічної грамотності; стиль використання авто (місто, траса, сімʼя, робота).

Уподобання й обмеження:
бюджет; тип кузова; паливо; марки; ставлення до битих авто зі США; допустимий пробіг; що для людини важливіше (надійність, комфорт, динаміка).

Активний пошук:
що людина шукає прямо зараз, вимоги, кандидати. Розділ ОБОВʼЯЗКОВО закривається: якщо людина написала, що купила авто або припинила пошук, очисти розділ, а підсумок перенеси одним рядком у Рішення.

Рішення:
журнал з датами: які авто людина дивилась, що відсіяла чи обрала і чому. До 10 рядків, свіжі витісняють старі.

Загальні правила нотатки:
- Свіже перемагає застаріле. Без повторів, без разових дрібниць.
- Дані конкретних звітів (кошториси, ціни, VIN, деталі пошкоджень) не копіюй: вони приходять окремо разом зі звітом.
- Порожній розділ лишай самим заголовком, не вигадуй вміст.
- Пиши стисло, рядками. Мʼякий ліміт 2500 символів: наближаючись до нього, стискай формулювання і викидай найменш цінне.
- Мовою, якою переважно пише користувач.
- НІКОЛИ не використовуй символ довгого тире.`;

/* Новий шлях памʼяті: модель повертає оновлену нотатку службовим блоком у тій
   самій відповіді, окремий виклик /api/memory для цього більше не обовʼязковий.
   Блок просять лише коли сторінка може нотатку зберегти (передала memory рядком,
   тобто користувач залогінений). Все після рядка-маркера користувач не бачить. */
const MEMORY_MARK = '[[MEMORY]]';
const MEMORY_TASK = `СЛУЖБОВИЙ БЛОК ПАМʼЯТІ (технічний, обовʼязковий):
- У САМОМУ КІНЦІ кожної відповіді додай окремий рядок ${MEMORY_MARK} і одразу після нього повну оновлену нотатку памʼяті про користувача. Поточна нотатка показана в блоці ПРОФІЛЬ ВЛАСНИКА (якщо її нема, почни нову).
${NOTE_SPEC}
- Онови нотатку фактами про ЛЮДИНУ з цієї розмови. Якщо нового про людину нічого нема, поверни нотатку без змін, але рядок ${MEMORY_MARK} і нотатку виведи однаково.
- Користувач цього блока НЕ бачить: сервіс відрізає все, починаючи з рядка ${MEMORY_MARK}. У видимій частині відповіді не згадуй ні блок, ні те, що ведеш нотатку.`;


/* Наскрізний хвіст розмов: без вгадування смислу. У блок ідуть УСІ репліки
   поточного звіту плюс 4 найсвіжіші з інших, разом не більше 8. Жодної
   фільтрації за словами чи марками. Порожній хвіст: блока нема взагалі. */
const TURNS = list => list.length
  ? `НЕДАВНІ РОЗМОВИ (останні репліки ЦІЄЇ Ж людини у чатах її звітів, найсвіжіші внизу):
${list.map(t => '- [' + (t.title || 'звіт без назви') + (t.same ? ', цей самий звіт' : '') + (t.at ? ', ' + t.at.slice(0, 10) : '') + '] ' + (t.role === 'user' ? 'людина' : 'помічник') + ': ' + t.text).join('\n')}
- Це розмови цієї самої людини, продовжуй їхню нитку природно. Згадуючи розмову з іншого звіту, називай те авто за назвою зі списку.
- Якщо свіжа репліка суперечить старішій або нотатці профілю, вір свіжим словам.`
  : '';


/* Звіти, які людина явно приклала до питання через @: до двох, без фото */
const REFS_TASK = `ДОДАНІ ЗВІТИ ДЛЯ ПОРІВНЯННЯ:
- До питання прикладені збережені звіти цієї ж людини (у першому повідомленні, "Додані користувачем звіти для порівняння"). Це прямий запит порівняти їх з авто цього звіту або відповісти по них.
- Порівнюй структуровано по пунктах: стан і пошкодження, пробіг, двигун і привід, комплектація, ризики й оцінка, підсумок під ключ (де є). Заверши ясним висновком: який варіант виглядає кращим для цієї людини і чому.
- Зважай на вік доданого звіту (created_at): якщо йому понад ~60 днів, ціни й доступність застарілі, порівнюй характеристики, а не ціни.`;

/* Фрагмент звіту, який людина виділила кнопкою "Спитати про це" */
const QUOTE_TASK = q => `ЦИТАТА ЗІ ЗВІТУ:
"${q}"
- Користувач виділив САМЕ цей фрагмент звіту і питає про нього. Відповідай про цей фрагмент у контексті всього звіту, а не загальними словами про авто.`;

const SYSTEM = (product, memory, wantMemory, turns, hasRefs, quote) => `${product === 'check' ? DOMAIN_CHECK : DOMAIN_IMPORT}

${OWNER(memory)}
${TURNS(turns) ? '\n' + TURNS(turns) + '\n' : ''}${hasRefs ? '\n' + REFS_TASK + '\n' : ''}${quote ? '\n' + QUOTE_TASK(quote) + '\n' : ''}
ІНШІ ПРОРАХУНКИ КОРИСТУВАЧА (context.other_reports_of_this_user, якщо є):
- Це авто, які ця людина вже аналізувала: назва, дата аналізу (analyzed_at і days_ago), пошкодження, пробіг, підсумок під ключ і ризик на момент збереження (totals), хвіст переписки (chat_tail).
- Використовуй їх для порівнянь ("чи це краще за ту BMW?") і для розуміння, що людина шукає.
- ЧАС ОБОВ'ЯЗКОВИЙ: згадуючи інший звіт, називай, коли він робився ("ти дивився її 3 місяці тому"). Якщо звіту понад ~60 днів: ціни й доступність лота застарілі, скажи це прямо і порівнюй лише характеристики авто (стан, пробіг, комплектацію, ризик), а не ціни. Лот зі старого звіту майже напевно вже проданий, не пропонуй "взяти його натомість".
- ПОРІВНЯННЯ НА ЗАПИТ: якщо просять порівняти це авто з конкретним із минулих ("порівняй з тією BMW"), знайди його в списку за назвою чи лотом і порівняй структуровано по пунктах: пошкодження і проблемні прапорці, пробіг, двигун і привід, комплектація, ризик, підсумок під ключ (якщо totals є в обох). Заверши чіткою відповіддю, яке авто виглядає кращою покупкою і чому. Памʼятай: фото того авто ти не бачиш, порівнюєш за даними звіту.
- Якщо потрібного авто в списку нема (у контексті лише 12 останніх), скажи чесно, що не бачиш такого прорахунку, і попроси уточнити назву або відкрити той звіт у кабінеті.
- Якщо звітів нема в context, значить людина не залогінена або інших прорахунків не існує: не вигадуй їх.

Правила:
- Відповідай тією мовою, якою користувач поставив останнє питання: російською на російське, українською на українське, англійською на англійське. Тексти звіту можуть бути українською, російською або англійською, це не впливає на мову твоєї відповіді.
- Коротко і по суті: 1-4 речення на просте питання. Списки лише коли їх реально просять або без них незрозуміло.
- Спирайся на передані дані, фото, прикріплені файли і профіль власника. Не вигадуй фактів про це авто. Якщо чогось у даних немає або по фото не видно, скажи прямо і порадь, як перевірити.
- Усі ціни в доларах США, орієнтовні для ринку України. Підсумок "під ключ" вже порахований у totals, використовуй його.
- Можеш рахувати: наприклад, перерахувати підсумок з іншою ставкою (мито 10% від ставки, ПДВ 20% від ставки+мита+акцизу, акциз не змінюється).
- НІКОЛИ не використовуй символ довгого тире, пиши кому, двокрапку або крапку.
- Питання про саму людину, про її нинішнє авто і про її минулі прорахунки це частина твоєї теми, а не стороннє: відповідай на них по суті. Одним реченням і поверненням до теми відповідай лише на справді сторонні питання (погода, політика, теми поза автомобілями).${wantMemory ? '\n\n' + MEMORY_TASK : ''}`;


export default async function handler(req, res) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
  if (!process.env.OPENAI_API_KEY) {
    return res.status(500).json({ error: 'OPENAI_API_KEY не налаштований у Vercel' });
  }

  try {
    const body = req.body || {};
    /* сторінка Check шле report/others окремими полями, сторінка Import
       кладе все в context: приймаємо обидві форми */
    const { messages, photos } = body;
    const product = body.product === 'check' ? 'check' : 'import';
    /* памʼять шлють обидві сторінки полем memory: рядок значить, що користувач
       залогінений і сторінка вміє зберегти оновлену нотатку (для гостей memory
       приходить null і службовий блок памʼяті в моделі не просимо) */
    const wantMemory = typeof body.memory === 'string';
    const memory = wantMemory ? body.memory.trim().slice(0, 3000) : '';

    /* хвіст розмов: чистимо від сміття, далі відбір без вгадування смислу:
       усі репліки цього звіту плюс 4 найсвіжіші з інших, разом до 8 */
    const curId = typeof body.report_id === 'string' && body.report_id ? body.report_id : null;
    const cleanTurns = (Array.isArray(body.recent_turns) ? body.recent_turns : [])
      .filter(t => t && typeof t === 'object' && (t.role === 'user' || t.role === 'assistant') && typeof t.text === 'string' && t.text.trim())
      .slice(-24)
      .map(t => ({
        same: curId !== null && t.report_id === curId,
        title: typeof t.title === 'string' ? t.title.replace(/\u2014/g, ',').slice(0, 80) : null,
        at: typeof t.at === 'string' ? t.at.slice(0, 24) : null,
        role: t.role,
        text: t.text.trim().replace(/\u2014/g, ',').slice(0, 280),
      }));
    const freshOthers = cleanTurns.filter(t => !t.same).slice(-4);
    const turns = cleanTurns.filter(t => t.same || freshOthers.includes(t)).slice(-8);

    /* звіти, прикладені через @: білий список полів, максимум два, без фото */
    const refs = (Array.isArray(body.referenced_reports) ? body.referenced_reports : [])
      .filter(r => r && typeof r === 'object' && !Array.isArray(r))
      .slice(0, 2)
      .map(r => {
        const pick = {};
        for (const k of ['id', 'title', 'kind', 'created_at', 'vehicle', 'verdict', 'risks', 'flags', 'damage_note', 'totals', 'meta']) {
          if (r[k] !== undefined && r[k] !== null) pick[k] = r[k];
        }
        return pick;
      })
      .filter(r => Object.keys(r).length);

    /* цитата зі звіту: сторінка ріже по 500, тут страхувальна межа */
    const quoted = typeof body.quoted_text === 'string' ? body.quoted_text.trim().replace(/\u2014/g, ',').slice(0, 600) : '';
    /* внутрішні дані Score (активний і тіньовий breakdown): чат їх повністю
       ігнорує і не згадує, жодним шляхом: ні через context, ні через інші
       звіти користувача */
    const stripV2 = o => {
      if (!o || typeof o !== 'object' || Array.isArray(o)) return o;
      const c = { ...o };
      delete c.score_facts;
      delete c.score_v2_preview;
      delete c.score_breakdown_v2;
      delete c.score_breakdown;
      delete c.score_breakdown_shadow;
      delete c.active_score_version;
      return c;
    };
    let context = stripV2(body.context || body.report || {});
    if (context && Array.isArray(context.other_reports_of_this_user)) {
      context = { ...context, other_reports_of_this_user: context.other_reports_of_this_user.map(stripV2) };
    }
    if (Array.isArray(body.others) && body.others.length) {
      context = Object.assign({}, context, { other_reports_of_this_user: body.others.slice(0, 12).map(stripV2) });
    }

    const sanitizeBlocks = content => {
      let nImg = 0, nFile = 0;
      const blocks = content.map(b => {
        if (b && b.type === 'text' && typeof b.text === 'string' && b.text.trim()) {
          return { type: 'text', text: b.text.slice(0, 4000) };
        }
        if (b && b.type === 'image_url' && typeof b.image_url?.url === 'string') {
          const u = b.image_url.url;
          const okUrl = /^https:\/\//.test(u) || /^data:image\/(?:jpeg|png|webp);base64,/.test(u);
          if (okUrl && u.length <= 4500000 && ++nImg <= 4) {
            return { type: 'image_url', image_url: { url: u, detail: 'high' } };
          }
        }
        if (b && b.type === 'file' && typeof b.file?.file_data === 'string') {
          const d = b.file.file_data;
          if (/^data:application\/pdf;base64,/.test(d) && d.length <= 3600000 && ++nFile <= 2) {
            return { type: 'file', file: { filename: String(b.file.filename || 'document.pdf').slice(0, 80), file_data: d } };
          }
        }
        return null;
      }).filter(Boolean);
      return blocks.length ? blocks : null;
    };
    const hist = (Array.isArray(messages) ? messages : [])
      .slice(-12)
      .map(m => {
        if (!m || (m.role !== 'user' && m.role !== 'assistant')) return null;
        if (typeof m.content === 'string') {
          return m.content.trim() ? { role: m.role, content: m.content.slice(0, 4000) } : null;
        }
        if (Array.isArray(m.content)) {
          const blocks = sanitizeBlocks(m.content);
          return blocks ? { role: m.role, content: blocks } : null;
        }
        return null;
      })
      .filter(Boolean);
    if (!hist.length || hist[hist.length - 1].role !== 'user') {
      return res.status(400).json({ error: 'Порожнє питання' });
    }

    /* фото: https-посилання аукціону дешеві (detail low), data:-фото обмежуємо трьома */
    const raw = Array.isArray(photos) ? photos.filter(u => typeof u === 'string') : [];
    const https = raw.filter(u => /^https:\/\//.test(u)).slice(0, 6);
    const datas = https.length ? [] : raw.filter(u => /^data:image\/(?:jpeg|png|webp);base64,/.test(u)).slice(0, 3);
    const imgs = [...https, ...datas];

    /* дата потрібна службовому блоку памʼяті (журнал Рішення ведеться з датами)
       і оцінці свіжості інших звітів */
    const ctxText = 'Сьогодні: ' + new Date().toISOString().slice(0, 10) + '\nДані по цьому авто (JSON):\n' + JSON.stringify(context || {}).slice(0, 42000)
      + (refs.length ? '\n\nДодані користувачем звіти для порівняння (JSON):\n' + JSON.stringify(refs).slice(0, 24000) : '');
    const primer = {
      role: 'user',
      content: [
        ...imgs.map(u => ({ type: 'image_url', image_url: { url: u, detail: 'low' } })),
        { type: 'text', text: ctxText },
      ],
    };

    /* для чату важлива швидкість: reasoning мінімальний, окремо від analyze */
    const EFFORT = process.env.CHAT_REASONING_EFFORT || 'low';
    const modelBody = (withEffort = true) => {
      const b = {
        /* CHAT_MODEL дозволяє поставити чату швидшу/дешевшу модель окремо від analyze */
        model: process.env.CHAT_MODEL || process.env.OPENAI_MODEL || 'gpt-5.6-terra',
        max_completion_tokens: 2500,
        messages: [
          { role: 'system', content: SYSTEM(product, memory, wantMemory, turns, refs.length > 0, quoted) },
          primer,
          { role: 'assistant', content: 'Прийняв, я вивчив дані і фото цього авто. Питай.' },
          ...hist,
        ],
      };
      if (withEffort && EFFORT !== 'off') b.reasoning_effort = EFFORT;
      return b;
    };

    const callModel = async (body, ms) => {
      const ctl = new AbortController();
      const t = setTimeout(() => ctl.abort(), ms);
      try {
        const resp = await fetch('https://api.openai.com/v1/chat/completions', {
          signal: ctl.signal,
          method: 'POST',
          headers: {
            'content-type': 'application/json',
            authorization: 'Bearer ' + process.env.OPENAI_API_KEY,
          },
          body: JSON.stringify(body),
        });
        return await resp.json();
      } finally { clearTimeout(t); }
    };

    const t0 = Date.now();
    let data = await callModel(modelBody(), 50000);

    /* модель не підтримує reasoning_effort → пробуємо без нього */
    if (data?.error && /reasoning_effort|unknown|unsupported|unrecognized/i.test(String(data.error.message || ''))) {
      data = await callModel(modelBody(false), Math.max(15000, 52000 - (Date.now() - t0)));
    }

    console.log('[chat]', product, '| photos', imgs.length,
      '| hist', hist.length,
      '| memory', memory.length,
      '| turns', turns.length + '/' + cleanTurns.length,
      '| refs', refs.length,
      '| quote', quoted.length,
      '| ai', Date.now() - t0, 'ms',
      '| tokens', JSON.stringify(data?.usage || {}));

    if (data.error) {
      return res.status(502).json({ error: 'AI: ' + (data.error.message || 'помилка запиту') });
    }
    let reply = (data.choices?.[0]?.message?.content || '').trim();
    /* службовий блок памʼяті: все після маркера це нотатка, користувачу її не показуємо.
       Блока нема або він порожній: просто не оновлюємо памʼять цього разу, без помилок */
    let memoryUpdate = null;
    const mi = reply.indexOf(MEMORY_MARK);
    if (mi !== -1) {
      const note = reply.slice(mi + MEMORY_MARK.length).replace(/\u2014/g, ',').trim().slice(0, 2800);
      reply = reply.slice(0, mi).trim();
      if (note && note !== memory) memoryUpdate = note;
    }
    if (!reply) {
      return res.status(502).json({ error: 'AI повернув порожню відповідь, спробуй ще раз' });
    }
    return res.status(200).json(memoryUpdate ? { reply, memory_update: memoryUpdate } : { reply });
  } catch (e) {
    if (e.name === 'AbortError') {
      return res.status(504).json({ error: 'Відповідь не встигла за відведений час, спробуй ще раз' });
    }
    return res.status(500).json({ error: 'Внутрішня помилка: ' + e.message });
  }
}
